<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pv_sisto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view_sales_Activity"></activity>
        <activity android:name=".view_Products_Activity" />
        <activity android:name=".homeActivity" />
        <activity android:name=".delete_Product_Activity" />
        <activity android:name=".add_user_Activity" />
        <activity android:name=".add_admin_Activity" />
        <activity android:name=".update_Product_Activity" />
        <activity android:name=".add_Product_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>